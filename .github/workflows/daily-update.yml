name: Daily Level Intelligence Update
on:
  schedule:
    - cron: "30 5 * * *"
  workflow_dispatch:
permissions:
  contents: write
concurrency:
  group: daily-level-intelligence
  cancel-in-progress: true
jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Collect Russian intelligence
        run: python -u collector.py
        timeout-minutes: 10
      - name: Collect Global intelligence
        run: python -u global_collector.py
        timeout-minutes: 10
      - name: Commit intelligence data
        run: |
          git config user.name "Level Intelligence"
          git config user.email "actions@users.noreply.github.com"
          git add news.json global_news.json
          git diff --cached --quiet || git commit -m "Update Level and Global intelligence"
          git push
